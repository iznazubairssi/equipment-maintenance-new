{
  "name": "equipment-maintenance",
  "version": "1.0.0",
  "description": "Equipment Maintenance CAP Application",
  "dependencies": {
    "@cap-js/postgres": "^2.0.5",
    "@sap/cds": "^9",
    "@sap/xssec": "^3",
    "csv-parse": "^5.5.0",
    "express": "^4.21.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@sap/cds-dk": "^9.4.1",
    "@sap/ux-specification": "^1.120.1"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds-serve --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "postgres",
        "impl": "@cap-js/postgres",
        "vcap": {
          "name": "Postgres-DB"
        }
      },
      "auth": {
        "kind": "dummy"
      }
    }
  }
}
